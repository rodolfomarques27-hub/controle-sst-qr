# Histórico do verificador experimental

Esta pasta guarda o patch histórico da etapa de desenvolvimento do OCR experimental para listas de presença.

O arquivo não é executável e não participa do build da aplicação.

A implementação preservada está em:

`src/experimental/verificador-lista-presenca`
